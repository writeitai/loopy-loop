# Requirements

Binding **WHAT**: capabilities, properties, constraints, and non-goals of the complete
intended system — highest abstraction, no mechanisms. Exactly one document is current
at any time and names what it supersedes. Files marked `DRAFT` (written by
`design_shape`) are candidates, not binding. See `plan/README.md`.

<!-- No requirements yet. -->
