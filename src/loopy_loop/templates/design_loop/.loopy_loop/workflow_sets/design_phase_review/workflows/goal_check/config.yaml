enabled: true
priority: 20
run_every: 1
must_follow: phase_reviewer
not_before_iteration: 1
emits_goal_check: true
description: |
  Run the set's shipped checks and terminate the child when a complete, digest-pinned
  sufficiency report exists.
