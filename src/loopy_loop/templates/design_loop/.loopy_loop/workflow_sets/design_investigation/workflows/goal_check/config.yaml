enabled: true
priority: 30
run_every: 1
must_follow: research_critic
not_before_iteration: 4
run_after_successes:
  workflow_id: investigator
  every: 3
emits_goal_check: true
description: |
  Run the set's shipped checks and terminate the child when the investigation round is
  complete, verified, and durable.
