enabled: true
priority: 90
run_every: 1
must_follow: eval_reviewer
run_when_requested: true
description: |
  Run the session's checks when an eval is requested and record the result as
  advisory evidence for outer.
